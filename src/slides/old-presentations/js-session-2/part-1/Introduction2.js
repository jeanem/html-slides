import React from 'react';

function Slide(props) {
  return (
    <div>
      <p>Allow us to:</p>

      <ul>
        <li>Write programs as a series of actions broken into small steps</li>
        <li>Reuse code instead of rewriting it</li>
        <li>Keep variable namespace clean</li>
        <li>Test parts of the application in isolation from each other</li>
        <li> Make it easier to work on projects together</li>
      </ul>

      <p>
        Many different ways to declare and invoke functions which we’ll review
        today
      </p>
    </div>
  );
}

export default Slide;
