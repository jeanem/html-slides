import React from 'react';

function Slide(props) {
  return (
    <div>
      <p>
        In JavaScript, values are chunks of bits that represent information.
        Values are how JavaScript organizes the millions or billions of bits it
        may be working with.
      </p>
      <ul>
        <li>Each value has a type</li>
        <li>And that type determines the role</li>
        <li>A value is created by invoking its name</li>
      </ul>
    </div>
  );
}

export default Slide;
