import React from 'react';

function Slide(props) {
  return (
    <div>
      <p>
        The previous chapter introduced the standard function{' '}
        <code>Math.min</code> that returns its smallest argument. We can build
        something like that now. Write a function min that takes two arguments
        and returns their minimum.
      </p>
      <p>
        Source:{' '}
        <a href="https://eloquentjavascript.net/03_functions.html#i_XTmO7z7MPq">
          Eloquent JavaScript
        </a>
      </p>
    </div>
  );
}

export default Slide;
