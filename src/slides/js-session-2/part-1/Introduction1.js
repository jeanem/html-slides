import React from 'react';

function Slide(props) {
  return (
    <div>
      <p>
        Functions are one of the core concepts in JavaScript programming - as
        well as many other programming languages. A function is a code snippet
        that can be called by itself, other code, or a variable referring to the
        function.
      </p>
    </div>
  );
}

export default Slide;
